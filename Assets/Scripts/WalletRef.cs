using UnityEngine;

public class WalletRef : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public WalletData walletRef;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
